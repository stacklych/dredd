openapi: "3.0.0"
info:
  title: Warning API
  version: "1.0"
paths:
  /machines:
    get:
      parameters:
        - name: Authorization
          in: header
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            "application/json; charset=utf-8":
              example:
                - type: bulldozer
                  name: willy
